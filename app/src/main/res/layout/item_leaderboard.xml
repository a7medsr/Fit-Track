<?xml version="1.0" encoding="utf-8"?>
<!-- One row of the weekly board. The freshness line is not decoration: a score
     is published by its owner's own phone, so a stalled row usually means they
     have not opened the app, not that they stopped moving. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/space_sm"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/bg_card"
    android:padding="@dimen/space_md">

    <TextView
        android:id="@+id/rank"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.FitTrack.Section"
        android:textColor="@color/text_tertiary"
        tools:text="1" />

    <FrameLayout
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginStart="@dimen/space_sm">

        <TextView
            android:id="@+id/memberInitials"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="13sp"
            android:fontFamily="sans-serif-medium"
            android:includeFontPadding="false"
            tools:text="AS" />

        <ImageView
            android:id="@+id/memberAvatar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:importantForAccessibility="no" />

    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/space_md"
        android:orientation="vertical">

        <TextView
            android:id="@+id/memberName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textAppearance="@style/TextAppearance.FitTrack.Section"
            android:textSize="15sp"
            tools:text="Ahmed Samir" />

        <TextView
            android:id="@+id/memberFreshness"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.FitTrack.Caption"
            android:textSize="12sp"
            android:textColor="@color/text_tertiary"
            tools:text="updated 3d ago" />

    </LinearLayout>

    <TextView
        android:id="@+id/memberValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/space_sm"
        android:textAppearance="@style/TextAppearance.FitTrack.Section"
        android:textColor="@color/brand_bright"
        tools:text="42,180" />

</LinearLayout>
