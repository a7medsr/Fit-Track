<?xml version="1.0" encoding="utf-8"?>
<!--
  One conversation row. The bubble's gravity, background and text colour are set
  in the adapter, so user and assistant share a single layout.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/space_sm"
    android:orientation="vertical">

    <TextView
        android:id="@+id/bubble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="300dp"
        android:background="@drawable/bg_bubble_assistant"
        android:paddingHorizontal="@dimen/space_lg"
        android:paddingVertical="@dimen/space_md"
        android:textSize="15sp"
        android:lineSpacingExtra="2dp"
        android:textColor="@color/text_primary"
        android:textIsSelectable="true"
        android:text="@string/em_dash" />

    <!-- Confirm chip for a proposed write. Reads run without one. -->
    <LinearLayout
        android:id="@+id/confirmRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_sm"
        android:orientation="vertical"
        android:background="@drawable/bg_card"
        android:padding="@dimen/space_md"
        android:visibility="gone">

        <TextView
            android:id="@+id/confirmPrompt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxWidth="280dp"
            android:text="@string/em_dash"
            android:textAppearance="@style/TextAppearance.FitTrack.Caption"
            android:textColor="@color/text_primary" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_md"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/confirmBtn"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:gravity="center"
                android:paddingHorizontal="@dimen/space_lg"
                android:background="@drawable/bg_button_primary"
                android:text="@string/chat_confirm"
                android:textColor="@color/on_brand"
                android:textSize="13sp"
                android:textStyle="bold"
                android:focusable="true"
                android:clickable="true" />

            <TextView
                android:id="@+id/cancelBtn"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_marginStart="@dimen/space_sm"
                android:gravity="center"
                android:paddingHorizontal="@dimen/space_lg"
                android:background="@drawable/bg_button_secondary"
                android:text="@string/action_cancel"
                android:textColor="@color/text_secondary"
                android:textSize="13sp"
                android:focusable="true"
                android:clickable="true" />

        </LinearLayout>

    </LinearLayout>

    <!-- Outcome once the chip has been answered. -->
    <TextView
        android:id="@+id/actionOutcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_xs"
        android:textAppearance="@style/TextAppearance.FitTrack.Overline"
        android:textColor="@color/text_tertiary"
        android:visibility="gone"
        android:text="@string/em_dash" />

</LinearLayout>
